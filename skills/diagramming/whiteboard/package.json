{
  "name": "whiteboard-render",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Headless render dependencies for whiteboard (scripts/render.mjs). Run `npm install` here once to enable PNG/SVG export; the skill works JSON-only without it.",
  "scripts": {
    "render": "node scripts/render.mjs"
  },
  "dependencies": {
    "@excalidraw/excalidraw": "^0.18.0",
    "esbuild": "^0.24.0",
    "puppeteer-core": "^23.0.0"
  }
}
